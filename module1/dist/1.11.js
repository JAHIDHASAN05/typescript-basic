"use strict";
{
    const age = 142;
    if (age >= 18) {
        console.log('adult');
    }
    else {
        console.log('not adult');
    }
    const isAdult = age >= 18 ? 'adut' : "Not Adult";
    console.log({ isAdult });
}
